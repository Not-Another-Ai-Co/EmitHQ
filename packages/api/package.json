{
  "name": "@emithq/api",
  "version": "0.1.0",
  "private": true,
  "description": "EmitHQ REST API server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "echo 'api: using tsx for production — no tsc build needed'",
    "start": "tsx src/server.ts",
    "start:worker": "tsx src/worker.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@clerk/backend": "^3.2.3",
    "@emithq/core": "*",
    "@hono/clerk-auth": "^3.1.0",
    "@hono/node-server": "^1.19.11",
    "@sentry/node": "^10.43.0",
    "hono": "^4.12.7",
    "stripe": "^20.4.1",
    "tsx": "^4.21.0"
  }
}
